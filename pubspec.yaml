name: ggiholland
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  file_picker: ^8.0.0
  csv: ^6.0.0
  shared_preferences: ^2.2.2
  cupertino_icons: ^1.0.8  # ← HIER moet het staan!
  flutter_svg: ^2.0.9  # ← VOEG TOE voor SVG support!


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/ggi_logo.svg
    - assets/ggi_logo_white.svg